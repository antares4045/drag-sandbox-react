import React from 'react'

const Circle = (props) => {
    const { pos, fill, r, stroke} = props
    return (
        <circle cx={pos.x} cy={pos.y} fill={fill} r={r} stroke={stroke || 'none'}></circle>
    )
}

export default Circle